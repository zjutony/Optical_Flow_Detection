# 20251009
● 整理了云栖大会相关文档，对LLM测评相关内容做了梳理  
● 优化了学术搜索工具的工作流程，更新了对用户复杂查询的直接搜索功能；  
● 整理了一下目前Agent评测上，主要的数据集和评测方法：Agent评测  

# 20251011
● 优化了google scholar search返回的内容格式，由于本身返回的是存在部分latex格式的内容，需要去除；  
● 测评了一些case学术搜索工具文档  
# 20251013-1016
● 优化了部分google scholar search工具返回内容格式化效果差的问题，由于google scholar的搜索结果是通过爬虫获取，只能拿到前端页面上显示的内容，在作者、abstract上都会存在缺失；  
    ○ 解决方法：通过重新解析论文link的内容，找到论文的具体细节；  
    ○ 目前将来源于arxiv的论文内容进行了整理，其他论文链接由于来源很丰富，只能通过爬虫形式获取，且大多来源于各种论文期刊会议官网，如果频繁请求存在被禁用风险，所以暂时不爬取  
● 持续补充一些case  
● 整理了一些搜索引擎Agent相关的论文和优化方法：  
    ○ Search-R1提出了LLM+搜索引擎结合的强化学习方法  
    ○ ZeroSearch 提出了用一个LLM来模拟虚拟搜索引擎的方法，让搜索Agent和虚拟搜索引擎进行交互，可以解决：  
        ■ api调用成本高  
        ■ 搜索引擎返回内容质量不稳定问题  
    ○ SSRL 提出了Self-Search的方式，不依赖外部搜索引擎来提升模型的能力  
        ■ 他假设模型自身还拥有丰富的知识没有被挖掘，所以可以让模型自己生成类似搜索引擎返回的内容；  
            ■ 他做了一些验证，证明了对于部分问题，模型自身其实已经有了很多知识，在多轮尝试时会输出正确答案；  
        ■ 但是和我们的信息收集场景不太符合，我们还是需要找到准确的信息，如果依靠模型来获取，没法解决模型出现的幻觉问题；  
    ○ TTRL 提出了模型自我学习的一种RL方法，核心在于让模型可以在一些没有明确label的测试集上进行训练，将LLM多次采样后的众数答案作为临时标准答案，然后计算采样的所有答案和临时标准答案是否相同，来计算奖励，并优化模型；  
        ■ 这个方法个人感觉可以在query生成上做一些优化；  
    ○ LLM-as-a-Judge 提到了一些使用LLM进行doc筛选时可以用的prompt策略（可以直接使用）  
        ■ 除此之外使用LLM作为筛选器时，主要还是依靠微调；  
            ■ 微调的问题在于数据集如何构建？  
    ○ SRDT-AutoRefine 提出了在“search+think”的环节中添加一个对文档的精炼refine操作，即“search+refine+think”的步骤，并在RL训练中，整合最终答案的reward和refine环节的reward，让模型有意识的学习refine的过程，有效解决了retrieve回的文档中的噪声问题；
        ■ 使用了F1方法来计算最终答案输出奖励  
    ○ S3：Search-Select-Serve 提出了单独训练Search模型，冻结生成模型的方法，并对数据进行整理，并使用了GenAcc的评价标准，来替代Exact Match（比较死板）的方法；
● 在信息收集Agent中，主要提升的能力应该是query生成（Search-R1、ZeroSearch SSRL 论文都提到了类似的方法）和文档召回优化（LLM-as-a-Judge ，SRDT-AutoRefine ）,S3：Search-Select-Serve 把query生成和文档召回一起优化了；
    ○ query生成需要和搜索引擎联动，基于生成的query，返回搜索结果，再通过最终模型返回结果来优化query生成策略；
    ○ 文档召回优化：
        ■ 特定方法优化一个judge LLM
        ■ 类似refine的做法


